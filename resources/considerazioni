- map -m 0 -AF
- uno_8bit sarebbe 0.0625
- sono stati provati sia jedi che nova
- una volta in errore è necessario resettare
- in ogni stato, tranne reset, l'unico bit di input considerato è il primo
- differenza percentuale tra file di partenza, script manuale e script random
- selettore_valvola a dc quando il datapath non segue il percorso delle valvole
- essendo i registri inizializzati a 0, il funzionamento può partire anche senza resettare
- la latenza è dovuta al controllo su ciò che esce dai registri (transizione reset -> fine)
- and sull'ultimo mux del ph per evitare che chiudi, da solo, faccia passare un potenziale valore valido quando c'è errore
